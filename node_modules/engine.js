
 
    var fs = require('fs');
    
    function watchFolder(directory){
    
        fs.watch(directory,function (event, targetFile){
            console.log ("event"+ event + "targetfile" + targetFile );
            //read targetFile
            
            //Parse targetFile
            
            //rm TargetFile
        });
        
    }


    exports.watchFolder = watchFolder;
    
